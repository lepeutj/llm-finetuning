# Install PyTorch separately with the wheel matching the target hardware.
# setup_gcp.sh installs the CUDA 12.4 build before this file.
transformers==4.57.3
trl==0.26.1
peft==0.17.1
bitsandbytes==0.48.2
datasets>=3.0,<5
accelerate>=1.4,<2
PyYAML>=6,<7
safetensors>=0.4,<1
